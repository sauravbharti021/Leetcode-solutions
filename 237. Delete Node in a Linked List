class Solution {
public:
    
    ListNode* root= NULL;
    void deleteNode(ListNode* node) {
       
        root= node->next;
        node->val= root->val;
        node->next= root->next;
        
    }
};
