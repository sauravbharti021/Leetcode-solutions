class Solution {
public:
    bool doesAliceWin(string s) {
        int vowels = 0;
        set<char> st{'a', 'e', 'i', 'o', 'u'};

        for(auto i: s){
            if(st.find(i)!=st.end()) vowels+=1;
        }

        if(vowels==0) return false;
        return true;
    }
};
