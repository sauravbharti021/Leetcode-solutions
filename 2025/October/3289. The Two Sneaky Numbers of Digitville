class Solution {
public:
    vector<int> getSneakyNumbers(vector<int>& nums) {
        int n= nums.size();
        vector<int> fre(n-2, 0), ans;
        for(int i=0;i<n;i++){
            if(fre[nums[i]]==1) ans.push_back(nums[i]);
            else fre[nums[i]]+=1;
        }

        return ans;
    }
};
