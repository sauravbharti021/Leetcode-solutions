import collections
class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        
        m1= collections.Counter(s)
        m2= collections.Counter(t)
        
        if(m1==m2):
            return True
        return False
